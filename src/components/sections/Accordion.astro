---
/**
 * Accordion
 * ---------
 * Expandable panel list (used for "What We Do" on the company page).
 * Pairs with src/scripts/accordion.ts and src/styles/accordion.css.
 */
import type { AccordionItem } from "@/types";

interface Props {
  eyebrow?: string;
  heading: string;
  headingDim?: string;
  items: AccordionItem[];
  image?: string;
  imageAlt?: string;
}

const {
  eyebrow,
  heading,
  headingDim,
  items,
  image = "/assets/client-proof/borrowed-luxe.webp",
  imageAlt = "Pinnacle Route at work",
} = Astro.props;
---

<section class="section relative overflow-hidden border-t border-line-soft">
  <div class="container-page grid gap-16 lg:grid-cols-2 lg:items-center">
    <!-- left: heading + accordion -->
    <div>
      {eyebrow && <p class="eyebrow mb-7" data-reveal>{eyebrow}</p>}
      <h2 class="display-sm max-w-[14ch]" data-reveal data-reveal-delay="1">
        {heading} {headingDim && <span class="dim">{headingDim}</span>}
      </h2>

      <div id="wwd" class="mt-12" data-reveal data-reveal-delay="1">
        {
          items.map((item, i) => (
            <div class="wwd-item pl-6">
              <button type="button" class="wwd-head flex items-center gap-4 w-full text-left py-4" aria-expanded={i === 0 ? "true" : "false"}>
                <span class="text-xs text-grey-3 tabular-nums">0{i + 1}</span>
                <span class="wwd-label display-sm !text-[clamp(1.5rem,3vw,2.25rem)] leading-none">{item.label}</span>
                <span class="wwd-arrow ml-2 text-xl">↗</span>
              </button>
              <div class="wwd-panel">
                <div class="grid sm:grid-cols-2 gap-x-10 gap-y-6 pb-8 pl-9">
                  <p class="text-sm leading-relaxed text-grey-2">{item.desc}</p>
                  <ul class="space-y-2.5">
                    {item.tags.map((t) => (
                      <li class="text-sm font-medium text-white">{t}</li>
                    ))}
                  </ul>
                </div>
              </div>
            </div>
          ))
        }
      </div>
    </div>

    <!-- right: image -->
    <div class="relative h-[480px] lg:h-[620px]" data-reveal data-reveal-delay="1">
      <div class="absolute inset-0 overflow-hidden mask-arch border border-line-soft bg-surface">
        <img src={image} alt={imageAlt} data-parallax="0.06" class="absolute inset-0 h-[115%] w-full object-cover -top-[7%] grayscale" />
      </div>
      <a href="/#services" class="pill pill-dark absolute bottom-6 left-6" data-magnetic="0.25">
        Services <span class="arrow">↗</span>
      </a>
    </div>
  </div>
</section>
